# AI 教材轉換器 - 部署指南

## 📦 部署選項

本專案支援多種部署方式，選擇最適合您的方案：

---

## 方式一：本地部署

### 前置需求
- Node.js 18+ 
- npm 或 yarn

### 部署步驟

1. **安裝依賴**
```bash
npm install
```

2. **設定環境變數**
```bash
# 複製範例檔案
copy .env.example .env

# 編輯 .env 檔案，設定您的 Gemini API Key
# GEMINI_API_KEY=your_actual_api_key_here
```

3. **啟動伺服器**
```bash
npm start
```

4. **訪問應用程式**
開啟瀏覽器訪問: `http://localhost:3000`

---

## 方式二：Docker 部署

### 前置需求
- Docker
- Docker Compose（可選）

### 使用 Docker Compose（推薦）

1. **設定環境變數**
```bash
copy .env.example .env
# 編輯 .env 填入 GEMINI_API_KEY
```

2. **啟動容器**
```bash
docker-compose up -d
```

3. **查看日誌**
```bash
docker-compose logs -f
```

4. **停止容器**
```bash
docker-compose down
```

### 使用 Docker

1. **建立映像**
```bash
docker build -t ai-courseware-converter .
```

2. **運行容器**
```bash
docker run -d \
  -p 3000:3000 \
  -e GEMINI_API_KEY=your_api_key_here \
  --name courseware-app \
  ai-courseware-converter
```

---

## 方式三：Vercel 部署

### 前置需求
- Vercel 帳號
- Vercel CLI（可選）

### 使用 Vercel CLI

1. **安裝 Vercel CLI**
```bash
npm install -g vercel
```

2. **登入 Vercel**
```bash
vercel login
```

3. **部署**
```bash
vercel
```

4. **設定環境變數**
在 Vercel Dashboard 中設定：
- `GEMINI_API_KEY`: 您的 Google Gemini API Key
- `NODE_ENV`: production

### 使用 Vercel Dashboard

1. 前往 [Vercel Dashboard](https://vercel.com/dashboard)
2. 點擊「Import Project」
3. 連接您的 Git 儲存庫
4. 在「Environment Variables」中設定：
   - `GEMINI_API_KEY`
   - `NODE_ENV` = production
5. 點擊「Deploy」

---

## 方式四：Heroku 部署

1. **建立 Heroku 應用程式**
```bash
heroku create your-app-name
```

2. **設定環境變數**
```bash
heroku config:set GEMINI_API_KEY=your_api_key_here
heroku config:set NODE_ENV=production
```

3. **部署**
```bash
git push heroku main
```

4. **開啟應用程式**
```bash
heroku open
```

---

## 方式五：Railway 部署

1. 前往 [Railway](https://railway.app/)
2. 點擊「New Project」→「Deploy from GitHub repo」
3. 選擇您的儲存庫
4. 在「Variables」中設定：
   - `GEMINI_API_KEY`: 您的 API Key
   - `NODE_ENV`: production
5. Railway 會自動偵測並部署

---

## 🔑 環境變數說明

| 變數名稱 | 說明 | 必填 | 預設值 |
|---------|------|------|--------|
| `GEMINI_API_KEY` | Google Gemini API Key | ✅ 是 | - |
| `PORT` | 伺服器端口 | ❌ 否 | 3000 |
| `NODE_ENV` | 運行環境 | ❌ 否 | development |
| `ALLOWED_ORIGINS` | 允許的 CORS 來源 | ❌ 否 | localhost:3000 |
| `RATE_LIMIT_MAX` | 速率限制（次數） | ❌ 否 | 100 |
| `RATE_LIMIT_WINDOW_MS` | 速率限制（時間窗口，毫秒） | ❌ 否 | 900000 (15分鐘) |

---

## 🔒 安全性建議

### 生產環境
- ✅ 使用 HTTPS
- ✅ 設定正確的 CORS 來源
- ✅ 定期更換 API Key
- ✅ 監控 API 使用量

### API Key 管理
- ❌ 絕對不要將 API Key 提交到 Git
- ✅ 使用環境變數管理
- ✅ 使用不同的 Key 用於開發/生產環境

---

## 🧪 測試部署

部署完成後，請測試以下功能：

1. **健康檢查**
訪問: `https://your-domain.com/health`
應返回: `{"status":"OK",...}`

2. **API 狀態**
訪問: `https://your-domain.com/api/status`
應返回: `{"status":"OK","apiConfigured":true,...}`

3. **前端應用**
訪問: `https://your-domain.com`
應顯示應用程式首頁

4. **功能測試**
- 上傳測試文件
- 執行 AI 分析
- 下載 PDF/Word

---

## 📊 監控與日誌

### 查看日誌

**本地/Docker:**
```bash
# 本地
npm start  # 日誌會顯示在終端

# Docker
docker-compose logs -f
```

**Vercel:**
在 Dashboard → Deployments → 點擊部署 → Functions

**Heroku:**
```bash
heroku logs --tail
```

---

## ❓ 常見問題

### Q: API 返回 503 錯誤
A: 檢查 `GEMINI_API_KEY` 環境變數是否正確設定

### Q: CORS 錯誤
A: 檢查 `ALLOWED_ORIGINS` 是否包含您的前端網址

### Q: 部署後無法訪問
A: 檢查：
1. 環境變數是否正確
2. 端口設定是否正確
3. 防火牆設定

---

## 📞 支援

如有部署問題，請檢查：
1. [GitHub Issues](您的儲存庫/issues)
2. 專案文檔
3. 聯絡系統管理員
